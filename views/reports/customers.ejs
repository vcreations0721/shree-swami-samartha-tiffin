<%- include('../partials/header', { title: 'Customer-wise Report' }) %>

<h3>Customer-wise Payment Report</h3>

<table class="table table-bordered table-striped">
  <thead class="table-dark">
    <tr>
      <th>Customer</th>
      <th>Mobile</th>
      <th>Total Paid (₹)</th>
    </tr>
  </thead>
  <tbody>
    <% report.forEach(r => { %>
      <tr>
        <td><%= r.name %></td>
        <td><%= r.mobile %></td>
        <td><strong><%= r.totalPaid %></strong></td>
      </tr>
    <% }) %>
  </tbody>
</table>

<%- include('../partials/footer') %>
